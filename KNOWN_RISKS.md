# Known Risks & TODOs

## Wallet Integration
- **Lace `shieldedAddress` parsing**: In `frontend/src/walletAdapter.ts`, the decoding logic for extracting `ShieldedCoinPublicKey` and `ShieldedEncryptionPublicKey` from the Lace-provided `shieldedAddress` string is verified against self-generated (encode<->decode round-trip) test data only. Not yet verified against a live Lace-issued address. First live Deploy button click against a real connected wallet is the actual verification of this path â€” treat any failure there as highest priority, starting here.

## Secret Distribution
- **Issuer-Distributed `salt`**: Currently, the `salt` required for credential commitments is randomly generated by the issuer and must be securely distributed to the holder (along with the `credentialType` and `issuerId`). If the holder loses this `salt`, they can never prove the credential again, even if they have their `holderSecret`. A more robust future design would be to deterministically derive `salt = hash(holderSecret, publicIssuanceNonce)` so the holder only ever needs to backup their `holderSecret`. We opted for the issuer-distributed version now to avoid altering the compiled Compact circuit data structures in this pass, but this should be refactored before production to minimize holder data-loss vectors.

## Build / Types
The `@midnight-ntwrk/midnight-js-contracts` package (v4.1.1) expects the `CompiledContract` generic parameter `CompiledAssetsPath` to evaluate to `never` because paths aren't used in browser asset resolution. However, the `compactc` generated interface in `contracts/managed/` explicitly types it as `CompiledAssetsPath`. This mismatch throws `TS2769` during strict `tsc` checks. We've bypassed this with `@ts-expect-error` directly above `deployContract` and `findDeployedContract` calls. This is safe at runtime and tracked as a known SDK limitation.

## Deploy: Known Midnight SDK Toolchain Issue (RESOLVED)
**Error Message:** Error: Unexpected error: Error: expected instance of Pa at set maintenanceAuthority

**Resolution:** This error was confirmed to be caused by a major version mismatch between the locally running proof server and the frontend SDK. The official Quick Start guide originally pointed to proof-server:latest (which resolved to 7.x), while our frontend uses the 4.1.1 SDK built for Ledger 8. Swapping the local container to midnightntwrk/proof-server:8.1.0 successfully resolved the serialization mismatch, allowing the deploy transaction to proceed to the proof-generation phase.